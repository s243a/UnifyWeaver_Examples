// Generated by UnifyWeaver WAM-to-Rust transpilation
// Module: wam_rust_matrix_bench
// Date: 2026-05-17 12:17:44

pub mod value;
pub mod instructions;
pub mod state;

use std::collections::{HashMap, HashSet};
use value::Value;
use instructions::Instruction;
use state::WamState;

pub fn setup_foreign_predicates(vm: &mut WamState) {
    vm.register_foreign_predicate("category_ancestor/4");
    vm.register_foreign_native_kind("category_ancestor/4", "category_ancestor");
    vm.register_foreign_result_layout("category_ancestor/4", "tuple(1)");
    vm.register_foreign_result_mode("category_ancestor/4", "stream");
    vm.register_foreign_usize_config("category_ancestor/4", "max_depth", 10);
    vm.register_foreign_string_config("category_ancestor/4", "edge_pred", "category_parent");
}

pub fn foreign_pred_keys() -> HashSet<String> {
    let mut s = HashSet::new();
    s.insert("category_ancestor/4".to_string());
    s
}


use std::sync::OnceLock;

static SHARED_WAM: OnceLock<(Vec<Instruction>, HashMap<String, usize>)> = OnceLock::new();

fn get_shared_wam() -> &'static (Vec<Instruction>, HashMap<String, usize>) {
    SHARED_WAM.get_or_init(|| {
        let mut labels: HashMap<String, usize> = HashMap::new();
    labels.insert("dimension_n/1".to_string(), 1);
    labels.insert("max_depth/1".to_string(), 3);
    labels.insert("category_ancestor$power_sum_bound/3".to_string(), 5);
    labels.insert("category_ancestor$power_sum_selected/3".to_string(), 42);
    labels.insert("L_ite_else_1".to_string(), 55);
    labels.insert("L_ite_cont_1".to_string(), 60);
    labels.insert("category_ancestor$power_sum_grouped/3".to_string(), 62);
    labels.insert("category_ancestor$power_sum_grouped$grouped/2".to_string(), 76);
    labels.insert("category_ancestor$power_sum_grouped$grouped$sum_pairs/2".to_string(), 121);
    labels.insert("L_category_ancestor$power_sum_grouped$grouped$sum_pairs_2_2".to_string(), 126);
    labels.insert("category_ancestor$effective_distance_sum_selected/3".to_string(), 150);
    labels.insert("category_ancestor$effective_distance_sum_bound/3".to_string(), 159);
        let code: Vec<Instruction> = vec![
        Instruction::GetConstant(Value::Atom("5".to_string()), "A1".to_string()),
        Instruction::Proceed,
        Instruction::GetConstant(Value::Atom("10".to_string()), "A1".to_string()),
        Instruction::Proceed,
        Instruction::Allocate,
        Instruction::GetVariable("Y2".to_string(), "A1".to_string()),
        Instruction::GetVariable("Y3".to_string(), "A2".to_string()),
        Instruction::GetVariable("Y8".to_string(), "A3".to_string()),
        Instruction::PutValue("Y3".to_string(), "A1".to_string()),
        Instruction::BuiltinCall("nonvar/1".to_string(), 1),
        Instruction::PutVariable("Y1".to_string(), "A1".to_string()),
        Instruction::Call("dimension_n/1".to_string(), 1),
        Instruction::PutVariable("Y7".to_string(), "A1".to_string()),
        Instruction::PutStructure("-/1".to_string(), "A2".to_string()),
        Instruction::SetValue("Y1".to_string()),
        Instruction::BuiltinCall("is/2".to_string(), 2),
        Instruction::PutVariable("Y5".to_string(), "Y5".to_string()),
        Instruction::BeginAggregate("sum".to_string(), "Y5".to_string(), "Y8".to_string()),
        Instruction::PutValue("Y2".to_string(), "A1".to_string()),
        Instruction::PutValue("Y3".to_string(), "A2".to_string()),
        Instruction::PutVariable("Y4".to_string(), "A3".to_string()),
        Instruction::PutList("A4".to_string()),
        Instruction::SetValue("Y2".to_string()),
        Instruction::SetConstant(Value::Atom("[]".to_string())),
        Instruction::Call("category_ancestor/4".to_string(), 4),
        Instruction::PutVariable("Y6".to_string(), "A1".to_string()),
        Instruction::PutStructure("+/2".to_string(), "A2".to_string()),
        Instruction::SetValue("Y4".to_string()),
        Instruction::SetConstant(Value::Integer(1)),
        Instruction::BuiltinCall("is/2".to_string(), 2),
        Instruction::PutValue("Y5".to_string(), "A1".to_string()),
        Instruction::PutStructure("**/2".to_string(), "A2".to_string()),
        Instruction::SetValue("Y6".to_string()),
        Instruction::SetValue("Y7".to_string()),
        Instruction::BuiltinCall("is/2".to_string(), 2),
        Instruction::EndAggregate("Y5".to_string()),
        Instruction::PutValue("Y8".to_string(), "A1".to_string()),
        Instruction::PutConstant(Value::Atom("0".to_string()), "A2".to_string()),
        Instruction::BuiltinCall(">/2".to_string(), 2),
        Instruction::Deallocate,
        Instruction::Proceed,
        Instruction::Allocate,
        Instruction::GetVariable("Y1".to_string(), "A1".to_string()),
        Instruction::GetVariable("Y2".to_string(), "A2".to_string()),
        Instruction::GetVariable("Y3".to_string(), "A3".to_string()),
        Instruction::TryMeElse("L_ite_else_1".to_string()),
        Instruction::PutValue("Y2".to_string(), "A1".to_string()),
        Instruction::BuiltinCall("nonvar/1".to_string(), 1),
        /* unknown: cut_ite */
        Instruction::PutValue("Y1".to_string(), "A1".to_string()),
        Instruction::PutValue("Y2".to_string(), "A2".to_string()),
        Instruction::PutValue("Y3".to_string(), "A3".to_string()),
        Instruction::Call("category_ancestor$power_sum_bound/3".to_string(), 3),
        /* unknown: jump L_ite_cont_1 */
        Instruction::TrustMe,
        Instruction::PutValue("Y1".to_string(), "A1".to_string()),
        Instruction::PutValue("Y2".to_string(), "A2".to_string()),
        Instruction::PutValue("Y3".to_string(), "A3".to_string()),
        Instruction::Call("category_ancestor$power_sum_grouped/3".to_string(), 3),
        Instruction::Deallocate,
        Instruction::Proceed,
        Instruction::Allocate,
        Instruction::GetVariable("X4".to_string(), "A1".to_string()),
        Instruction::GetVariable("Y1".to_string(), "A2".to_string()),
        Instruction::GetVariable("Y2".to_string(), "A3".to_string()),
        Instruction::PutValue("X4".to_string(), "A1".to_string()),
        Instruction::PutVariable("Y3".to_string(), "A2".to_string()),
        Instruction::Call("category_ancestor$power_sum_grouped$grouped/2".to_string(), 2),
        Instruction::PutStructure("-/2".to_string(), "A1".to_string()),
        Instruction::SetValue("Y1".to_string()),
        Instruction::SetValue("Y2".to_string()),
        Instruction::PutValue("Y3".to_string(), "A2".to_string()),
        Instruction::BuiltinCall("member/2".to_string(), 2),
        Instruction::Deallocate,
        Instruction::Proceed,
        Instruction::Allocate,
        Instruction::GetVariable("Y2".to_string(), "A1".to_string()),
        Instruction::GetVariable("Y10".to_string(), "A2".to_string()),
        Instruction::PutVariable("Y1".to_string(), "A1".to_string()),
        Instruction::Call("dimension_n/1".to_string(), 1),
        Instruction::PutVariable("Y7".to_string(), "A1".to_string()),
        Instruction::PutStructure("-/1".to_string(), "A2".to_string()),
        Instruction::SetValue("Y1".to_string()),
        Instruction::BuiltinCall("is/2".to_string(), 2),
        Instruction::PutVariable("Y4".to_string(), "Y4".to_string()),
        Instruction::PutVariable("Y5".to_string(), "Y5".to_string()),
        Instruction::BeginAggregate("collect".to_string(), "A1".to_string(), "Y8".to_string()),
        Instruction::PutValue("Y2".to_string(), "A1".to_string()),
        Instruction::PutValue("Y4".to_string(), "A2".to_string()),
        Instruction::PutVariable("Y3".to_string(), "A3".to_string()),
        Instruction::PutList("A4".to_string()),
        Instruction::SetValue("Y2".to_string()),
        Instruction::SetConstant(Value::Atom("[]".to_string())),
        Instruction::Call("category_ancestor/4".to_string(), 4),
        Instruction::PutVariable("Y6".to_string(), "A1".to_string()),
        Instruction::PutStructure("+/2".to_string(), "A2".to_string()),
        Instruction::SetValue("Y3".to_string()),
        Instruction::SetConstant(Value::Integer(1)),
        Instruction::BuiltinCall("is/2".to_string(), 2),
        Instruction::PutValue("Y4".to_string(), "A1".to_string()),
        Instruction::BuiltinCall("nonvar/1".to_string(), 1),
        Instruction::PutValue("Y5".to_string(), "A1".to_string()),
        Instruction::PutStructure("**/2".to_string(), "A2".to_string()),
        Instruction::SetValue("Y6".to_string()),
        Instruction::SetValue("Y7".to_string()),
        Instruction::BuiltinCall("is/2".to_string(), 2),
        Instruction::PutStructure("-/2".to_string(), "A1".to_string()),
        Instruction::SetValue("Y4".to_string()),
        Instruction::SetValue("Y5".to_string()),
        Instruction::EndAggregate("A1".to_string()),
        Instruction::PutValue("Y8".to_string(), "A1".to_string()),
        Instruction::PutConstant(Value::Atom("[]".to_string()), "A2".to_string()),
        Instruction::BuiltinCall("\\=/2".to_string(), 2),
        Instruction::PutValue("Y8".to_string(), "A1".to_string()),
        Instruction::PutVariable("Y9".to_string(), "A2".to_string()),
        Instruction::BuiltinCall("keysort/2".to_string(), 2),
        Instruction::PutValue("Y9".to_string(), "A1".to_string()),
        Instruction::PutValue("Y10".to_string(), "A2".to_string()),
        Instruction::Deallocate,
        Instruction::Execute("category_ancestor$power_sum_grouped$grouped$sum_pairs/2".to_string()),
        /* unknown: switch_on_term 1 []:default 0 L_category_ancestor$power_sum_grouped$grouped$sum_pairs_2_2 */
        Instruction::TryMeElse("L_category_ancestor$power_sum_grouped$grouped$sum_pairs_2_2".to_string()),
        Instruction::GetConstant(Value::Atom("[]".to_string()), "A1".to_string()),
        Instruction::GetConstant(Value::Atom("[]".to_string()), "A2".to_string()),
        Instruction::Proceed,
        Instruction::TrustMe,
        Instruction::Allocate,
        Instruction::GetList("A1".to_string()),
        Instruction::UnifyVariable("X3".to_string()),
        Instruction::GetStructure("-/2".to_string(), "X3".to_string()),
        Instruction::UnifyVariable("X4".to_string()),
        Instruction::UnifyVariable("X5".to_string()),
        Instruction::UnifyVariable("X6".to_string()),
        Instruction::GetList("A2".to_string()),
        Instruction::UnifyVariable("X7".to_string()),
        Instruction::GetStructure("-/2".to_string(), "X7".to_string()),
        Instruction::UnifyValue("X4".to_string()),
        Instruction::UnifyVariable("X8".to_string()),
        Instruction::UnifyVariable("Y2".to_string()),
        Instruction::PutValue("X6".to_string(), "A1".to_string()),
        Instruction::PutValue("X4".to_string(), "A2".to_string()),
        Instruction::PutValue("X5".to_string(), "A3".to_string()),
        Instruction::PutValue("X8".to_string(), "A4".to_string()),
        Instruction::PutVariable("Y1".to_string(), "A5".to_string()),
        Instruction::Call("category_ancestor$power_sum_grouped$grouped$take_same_key/5".to_string(), 5),
        Instruction::PutValue("Y1".to_string(), "A1".to_string()),
        Instruction::PutValue("Y2".to_string(), "A2".to_string()),
        Instruction::Deallocate,
        Instruction::Execute("category_ancestor$power_sum_grouped$grouped$sum_pairs/2".to_string()),
        Instruction::Allocate,
        Instruction::GetVariable("X1".to_string(), "A1".to_string()),
        Instruction::GetVariable("X2".to_string(), "A2".to_string()),
        Instruction::GetVariable("X3".to_string(), "A3".to_string()),
        Instruction::PutValue("X1".to_string(), "A1".to_string()),
        Instruction::PutValue("X2".to_string(), "A2".to_string()),
        Instruction::PutValue("X3".to_string(), "A3".to_string()),
        Instruction::Deallocate,
        Instruction::Execute("category_ancestor$power_sum_selected/3".to_string()),
        Instruction::Allocate,
        Instruction::GetVariable("X1".to_string(), "A1".to_string()),
        Instruction::GetVariable("X2".to_string(), "A2".to_string()),
        Instruction::GetVariable("X3".to_string(), "A3".to_string()),
        Instruction::PutValue("X1".to_string(), "A1".to_string()),
        Instruction::PutValue("X2".to_string(), "A2".to_string()),
        Instruction::PutValue("X3".to_string(), "A3".to_string()),
        Instruction::Deallocate,
        Instruction::Execute("category_ancestor$power_sum_bound/3".to_string()),
        ];
        (code, labels)
    })
}

pub fn shared_wam_program() -> (Vec<Instruction>, HashMap<String, usize>) {
    let (code, labels) = get_shared_wam();
    (code.clone(), labels.clone())
}

// Strategy: lowered
// lowered_dimension_n_1 — lowered from dimension_n/1
pub fn lowered_dimension_n_1(vm: &mut WamState) -> bool {
    // get_constant 5, A1
    {
        let _a = vm.get_reg("A1").unwrap_or(Value::Uninit);
        if _a.is_unbound() {
            vm.trail_binding("A1");
            vm.put_reg("A1", Value::Integer(5));
        } else if _a != Value::Integer(5) {
            return false;
        }
    }
    return true;

}

// Strategy: lowered
// lowered_max_depth_1 — lowered from max_depth/1
pub fn lowered_max_depth_1(vm: &mut WamState) -> bool {
    // get_constant 10, A1
    {
        let _a = vm.get_reg("A1").unwrap_or(Value::Uninit);
        if _a.is_unbound() {
            vm.trail_binding("A1");
            vm.put_reg("A1", Value::Integer(10));
        } else if _a != Value::Integer(10) {
            return false;
        }
    }
    return true;

}

// Strategy: ffi_kernel (category_ancestor)
// category_ancestor/4 dispatched via CallForeign → execute_foreign_predicate

// Strategy: lowered
// lowered_category_ancestor_power_sum_bound_3 — lowered from category_ancestor$power_sum_bound/3
pub fn lowered_category_ancestor_power_sum_bound_3(vm: &mut WamState) -> bool {
    // allocate
    vm.step(&Instruction::Allocate);
    // get_variable Y2, A1
    if let Some(v) = vm.get_reg("A1") { vm.put_reg("Y2", v); }
    // get_variable Y3, A2
    if let Some(v) = vm.get_reg("A2") { vm.put_reg("Y3", v); }
    // get_variable Y8, A3
    if let Some(v) = vm.get_reg("A3") { vm.put_reg("Y8", v); }
    // put_value Y3, A1
    if let Some(v) = vm.get_reg("Y3") { vm.put_reg("A1", v); }
    // builtin_call nonvar/1 1
    if !vm.step(&Instruction::BuiltinCall("nonvar/1".to_string(), 1)) { return false; }
    // put_variable Y1, A1
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y1", v.clone());
        vm.put_reg("A1", v);
    }
    // call dimension_n/1
    {
        let saved_cp = vm.cp;
        if let Some(&pc) = vm.labels.get("dimension_n/1") {
            vm.pc = pc;
            if !vm.run() { return false; }
        } else { return false; }
        vm.cp = saved_cp;
    }
    // put_variable Y7, A1
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y7", v.clone());
        vm.put_reg("A1", v);
    }
    // put_structure -/1, A2 (delegate to step)
    if !vm.step(&Instruction::PutStructure("-/1".to_string(), "A2".to_string())) { return false; }
    // set_value Y1 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y1".to_string())) { return false; }
    // builtin_call is/2 2
    if !vm.step(&Instruction::BuiltinCall("is/2".to_string(), 2)) { return false; }
    // put_variable Y5, Y5
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y5", v.clone());
        vm.put_reg("Y5", v);
    }
    // put_value Y2, A1
    if let Some(v) = vm.get_reg("Y2") { vm.put_reg("A1", v); }
    // put_value Y3, A2
    if let Some(v) = vm.get_reg("Y3") { vm.put_reg("A2", v); }
    // put_variable Y4, A3
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y4", v.clone());
        vm.put_reg("A3", v);
    }
    // put_list A4 (delegate to step)
    if !vm.step(&Instruction::PutList("A4".to_string())) { return false; }
    // set_value Y2 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y2".to_string())) { return false; }
    // set_constant [] (delegate to step)
    if !vm.step(&Instruction::SetConstant(Value::Atom("[]".to_string()))) { return false; }
    // call category_ancestor/4 via foreign kernel
    if !vm.step(&Instruction::CallForeign("category_ancestor/4".to_string(), 4)) { return false; }
    // put_variable Y6, A1
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y6", v.clone());
        vm.put_reg("A1", v);
    }
    // put_structure +/2, A2 (delegate to step)
    if !vm.step(&Instruction::PutStructure("+/2".to_string(), "A2".to_string())) { return false; }
    // set_value Y4 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y4".to_string())) { return false; }
    // set_constant 1 (delegate to step)
    if !vm.step(&Instruction::SetConstant(Value::Integer(1))) { return false; }
    // builtin_call is/2 2
    if !vm.step(&Instruction::BuiltinCall("is/2".to_string(), 2)) { return false; }
    // put_value Y5, A1
    if let Some(v) = vm.get_reg("Y5") { vm.put_reg("A1", v); }
    // put_structure **/2, A2 (delegate to step)
    if !vm.step(&Instruction::PutStructure("**/2".to_string(), "A2".to_string())) { return false; }
    // set_value Y6 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y6".to_string())) { return false; }
    // set_value Y7 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y7".to_string())) { return false; }
    // builtin_call is/2 2
    if !vm.step(&Instruction::BuiltinCall("is/2".to_string(), 2)) { return false; }
    // put_value Y8, A1
    if let Some(v) = vm.get_reg("Y8") { vm.put_reg("A1", v); }
    // put_constant 0, A2
    vm.put_reg("A2", Value::Integer(0));
    // builtin_call >/2 2
    if !vm.step(&Instruction::BuiltinCall(">/2".to_string(), 2)) { return false; }
    // deallocate
    vm.step(&Instruction::Deallocate);
    return true;

}

// Strategy: lowered
// lowered_category_ancestor_power_sum_selected_3 — lowered from category_ancestor$power_sum_selected/3
pub fn lowered_category_ancestor_power_sum_selected_3(vm: &mut WamState) -> bool {
    // allocate
    vm.step(&Instruction::Allocate);
    // get_variable Y1, A1
    if let Some(v) = vm.get_reg("A1") { vm.put_reg("Y1", v); }
    // get_variable Y2, A2
    if let Some(v) = vm.get_reg("A2") { vm.put_reg("Y2", v); }
    // get_variable Y3, A3
    if let Some(v) = vm.get_reg("A3") { vm.put_reg("Y3", v); }
    // put_value Y2, A1
    if let Some(v) = vm.get_reg("Y2") { vm.put_reg("A1", v); }
    // builtin_call nonvar/1 1
    if !vm.step(&Instruction::BuiltinCall("nonvar/1".to_string(), 1)) { return false; }
    // put_value Y1, A1
    if let Some(v) = vm.get_reg("Y1") { vm.put_reg("A1", v); }
    // put_value Y2, A2
    if let Some(v) = vm.get_reg("Y2") { vm.put_reg("A2", v); }
    // put_value Y3, A3
    if let Some(v) = vm.get_reg("Y3") { vm.put_reg("A3", v); }
    // call category_ancestor$power_sum_bound/3
    {
        let saved_cp = vm.cp;
        if let Some(&pc) = vm.labels.get("category_ancestor$power_sum_bound/3") {
            vm.pc = pc;
            if !vm.run() { return false; }
        } else { return false; }
        vm.cp = saved_cp;
    }
    // put_value Y1, A1
    if let Some(v) = vm.get_reg("Y1") { vm.put_reg("A1", v); }
    // put_value Y2, A2
    if let Some(v) = vm.get_reg("Y2") { vm.put_reg("A2", v); }
    // put_value Y3, A3
    if let Some(v) = vm.get_reg("Y3") { vm.put_reg("A3", v); }
    // call category_ancestor$power_sum_grouped/3
    {
        let saved_cp = vm.cp;
        if let Some(&pc) = vm.labels.get("category_ancestor$power_sum_grouped/3") {
            vm.pc = pc;
            if !vm.run() { return false; }
        } else { return false; }
        vm.cp = saved_cp;
    }
    // deallocate
    vm.step(&Instruction::Deallocate);
    return true;

}

// Strategy: lowered
// lowered_category_ancestor_power_sum_grouped_3 — lowered from category_ancestor$power_sum_grouped/3
pub fn lowered_category_ancestor_power_sum_grouped_3(vm: &mut WamState) -> bool {
    // allocate
    vm.step(&Instruction::Allocate);
    // get_variable X4, A1
    if let Some(v) = vm.get_reg("A1") { vm.put_reg("X4", v); }
    // get_variable Y1, A2
    if let Some(v) = vm.get_reg("A2") { vm.put_reg("Y1", v); }
    // get_variable Y2, A3
    if let Some(v) = vm.get_reg("A3") { vm.put_reg("Y2", v); }
    // put_value X4, A1
    if let Some(v) = vm.get_reg("X4") { vm.put_reg("A1", v); }
    // put_variable Y3, A2
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y3", v.clone());
        vm.put_reg("A2", v);
    }
    // call category_ancestor$power_sum_grouped$grouped/2
    {
        let saved_cp = vm.cp;
        if let Some(&pc) = vm.labels.get("category_ancestor$power_sum_grouped$grouped/2") {
            vm.pc = pc;
            if !vm.run() { return false; }
        } else { return false; }
        vm.cp = saved_cp;
    }
    // put_structure -/2, A1 (delegate to step)
    if !vm.step(&Instruction::PutStructure("-/2".to_string(), "A1".to_string())) { return false; }
    // set_value Y1 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y1".to_string())) { return false; }
    // set_value Y2 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y2".to_string())) { return false; }
    // put_value Y3, A2
    if let Some(v) = vm.get_reg("Y3") { vm.put_reg("A2", v); }
    // builtin_call member/2 2
    if !vm.step(&Instruction::BuiltinCall("member/2".to_string(), 2)) { return false; }
    // deallocate
    vm.step(&Instruction::Deallocate);
    return true;

}

// Strategy: lowered
// lowered_category_ancestor_power_sum_grouped_grouped_2 — lowered from category_ancestor$power_sum_grouped$grouped/2
pub fn lowered_category_ancestor_power_sum_grouped_grouped_2(vm: &mut WamState) -> bool {
    // allocate
    vm.step(&Instruction::Allocate);
    // get_variable Y2, A1
    if let Some(v) = vm.get_reg("A1") { vm.put_reg("Y2", v); }
    // get_variable Y10, A2
    if let Some(v) = vm.get_reg("A2") { vm.put_reg("Y10", v); }
    // put_variable Y1, A1
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y1", v.clone());
        vm.put_reg("A1", v);
    }
    // call dimension_n/1
    {
        let saved_cp = vm.cp;
        if let Some(&pc) = vm.labels.get("dimension_n/1") {
            vm.pc = pc;
            if !vm.run() { return false; }
        } else { return false; }
        vm.cp = saved_cp;
    }
    // put_variable Y7, A1
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y7", v.clone());
        vm.put_reg("A1", v);
    }
    // put_structure -/1, A2 (delegate to step)
    if !vm.step(&Instruction::PutStructure("-/1".to_string(), "A2".to_string())) { return false; }
    // set_value Y1 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y1".to_string())) { return false; }
    // builtin_call is/2 2
    if !vm.step(&Instruction::BuiltinCall("is/2".to_string(), 2)) { return false; }
    // put_variable Y4, Y4
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y4", v.clone());
        vm.put_reg("Y4", v);
    }
    // put_variable Y5, Y5
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y5", v.clone());
        vm.put_reg("Y5", v);
    }
    // put_value Y2, A1
    if let Some(v) = vm.get_reg("Y2") { vm.put_reg("A1", v); }
    // put_value Y4, A2
    if let Some(v) = vm.get_reg("Y4") { vm.put_reg("A2", v); }
    // put_variable Y3, A3
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y3", v.clone());
        vm.put_reg("A3", v);
    }
    // put_list A4 (delegate to step)
    if !vm.step(&Instruction::PutList("A4".to_string())) { return false; }
    // set_value Y2 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y2".to_string())) { return false; }
    // set_constant [] (delegate to step)
    if !vm.step(&Instruction::SetConstant(Value::Atom("[]".to_string()))) { return false; }
    // call category_ancestor/4 via foreign kernel
    if !vm.step(&Instruction::CallForeign("category_ancestor/4".to_string(), 4)) { return false; }
    // put_variable Y6, A1
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y6", v.clone());
        vm.put_reg("A1", v);
    }
    // put_structure +/2, A2 (delegate to step)
    if !vm.step(&Instruction::PutStructure("+/2".to_string(), "A2".to_string())) { return false; }
    // set_value Y3 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y3".to_string())) { return false; }
    // set_constant 1 (delegate to step)
    if !vm.step(&Instruction::SetConstant(Value::Integer(1))) { return false; }
    // builtin_call is/2 2
    if !vm.step(&Instruction::BuiltinCall("is/2".to_string(), 2)) { return false; }
    // put_value Y4, A1
    if let Some(v) = vm.get_reg("Y4") { vm.put_reg("A1", v); }
    // builtin_call nonvar/1 1
    if !vm.step(&Instruction::BuiltinCall("nonvar/1".to_string(), 1)) { return false; }
    // put_value Y5, A1
    if let Some(v) = vm.get_reg("Y5") { vm.put_reg("A1", v); }
    // put_structure **/2, A2 (delegate to step)
    if !vm.step(&Instruction::PutStructure("**/2".to_string(), "A2".to_string())) { return false; }
    // set_value Y6 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y6".to_string())) { return false; }
    // set_value Y7 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y7".to_string())) { return false; }
    // builtin_call is/2 2
    if !vm.step(&Instruction::BuiltinCall("is/2".to_string(), 2)) { return false; }
    // put_structure -/2, A1 (delegate to step)
    if !vm.step(&Instruction::PutStructure("-/2".to_string(), "A1".to_string())) { return false; }
    // set_value Y4 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y4".to_string())) { return false; }
    // set_value Y5 (delegate to step)
    if !vm.step(&Instruction::SetValue("Y5".to_string())) { return false; }
    // put_value Y8, A1
    if let Some(v) = vm.get_reg("Y8") { vm.put_reg("A1", v); }
    // put_constant [], A2
    vm.put_reg("A2", Value::Atom("[]".to_string()));
    // builtin_call \=/2 2
    if !vm.step(&Instruction::BuiltinCall("\\=/2".to_string(), 2)) { return false; }
    // put_value Y8, A1
    if let Some(v) = vm.get_reg("Y8") { vm.put_reg("A1", v); }
    // put_variable Y9, A2
    {
        let v = Value::Unbound(format!("_V{}", vm.var_counter));
        vm.var_counter += 1;
        vm.put_reg("Y9", v.clone());
        vm.put_reg("A2", v);
    }
    // builtin_call keysort/2 2
    if !vm.step(&Instruction::BuiltinCall("keysort/2".to_string(), 2)) { return false; }
    // put_value Y9, A1
    if let Some(v) = vm.get_reg("Y9") { vm.put_reg("A1", v); }
    // put_value Y10, A2
    if let Some(v) = vm.get_reg("Y10") { vm.put_reg("A2", v); }
    // deallocate
    vm.step(&Instruction::Deallocate);
    // execute category_ancestor$power_sum_grouped$grouped$sum_pairs/2 (tail call)
    if let Some(&pc) = vm.labels.get("category_ancestor$power_sum_grouped$grouped$sum_pairs/2") {
        vm.pc = pc;
        return vm.run();
    }
    return false;

}

// Strategy: lowered
// lowered_category_ancestor_power_sum_grouped_grouped_sum_pairs_2 — lowered from category_ancestor$power_sum_grouped$grouped$sum_pairs/2
pub fn lowered_category_ancestor_power_sum_grouped_grouped_sum_pairs_2(vm: &mut WamState) -> bool {
    // get_constant [], A1
    {
        let _a = vm.get_reg("A1").unwrap_or(Value::Uninit);
        if _a.is_unbound() {
            vm.trail_binding("A1");
            vm.put_reg("A1", Value::Atom("[]".to_string()));
        } else if _a != Value::Atom("[]".to_string()) {
            return false;
        }
    }
    // get_constant [], A2
    {
        let _a = vm.get_reg("A2").unwrap_or(Value::Uninit);
        if _a.is_unbound() {
            vm.trail_binding("A2");
            vm.put_reg("A2", Value::Atom("[]".to_string()));
        } else if _a != Value::Atom("[]".to_string()) {
            return false;
        }
    }
    return true;

}

// Strategy: lowered
// lowered_category_ancestor_effective_distance_sum_selected_3 — lowered from category_ancestor$effective_distance_sum_selected/3
pub fn lowered_category_ancestor_effective_distance_sum_selected_3(vm: &mut WamState) -> bool {
    // allocate
    vm.step(&Instruction::Allocate);
    // get_variable X1, A1
    if let Some(v) = vm.get_reg("A1") { vm.put_reg("X1", v); }
    // get_variable X2, A2
    if let Some(v) = vm.get_reg("A2") { vm.put_reg("X2", v); }
    // get_variable X3, A3
    if let Some(v) = vm.get_reg("A3") { vm.put_reg("X3", v); }
    // put_value X1, A1
    if let Some(v) = vm.get_reg("X1") { vm.put_reg("A1", v); }
    // put_value X2, A2
    if let Some(v) = vm.get_reg("X2") { vm.put_reg("A2", v); }
    // put_value X3, A3
    if let Some(v) = vm.get_reg("X3") { vm.put_reg("A3", v); }
    // deallocate
    vm.step(&Instruction::Deallocate);
    // execute category_ancestor$power_sum_selected/3 (tail call)
    if let Some(&pc) = vm.labels.get("category_ancestor$power_sum_selected/3") {
        vm.pc = pc;
        return vm.run();
    }
    return false;

}

// Strategy: lowered
// lowered_category_ancestor_effective_distance_sum_bound_3 — lowered from category_ancestor$effective_distance_sum_bound/3
pub fn lowered_category_ancestor_effective_distance_sum_bound_3(vm: &mut WamState) -> bool {
    // allocate
    vm.step(&Instruction::Allocate);
    // get_variable X1, A1
    if let Some(v) = vm.get_reg("A1") { vm.put_reg("X1", v); }
    // get_variable X2, A2
    if let Some(v) = vm.get_reg("A2") { vm.put_reg("X2", v); }
    // get_variable X3, A3
    if let Some(v) = vm.get_reg("A3") { vm.put_reg("X3", v); }
    // put_value X1, A1
    if let Some(v) = vm.get_reg("X1") { vm.put_reg("A1", v); }
    // put_value X2, A2
    if let Some(v) = vm.get_reg("X2") { vm.put_reg("A2", v); }
    // put_value X3, A3
    if let Some(v) = vm.get_reg("X3") { vm.put_reg("A3", v); }
    // deallocate
    vm.step(&Instruction::Deallocate);
    // execute category_ancestor$power_sum_bound/3 (tail call)
    if let Some(&pc) = vm.labels.get("category_ancestor$power_sum_bound/3") {
        vm.pc = pc;
        return vm.run();
    }
    return false;

}
